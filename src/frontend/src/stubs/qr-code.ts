// Stub for @caffeineai/qr-code — package not yet installed
export function useQRScanner() {
  return {
    scanResult: null as string | null,
    isScanning: false,
    startScan: () => {},
    stopScan: () => {},
    error: null as string | null,
  };
}
