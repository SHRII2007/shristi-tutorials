import React from 'react';

const LoginPage = () => {
  return (
    <div>
      {/* Other code */} 
      <div className="w-full relative overflow-hidden flex items-center justify-center gap-3 px-6 py-4 rounded-2xl text-white font-display font-bold text-base cursor-pointer transition-all">
        {/* Content */}
      </div>
      {/* Other code */} 
      <div className="w-full flex items-center justify-center gap-2 px-6 py-3.5 rounded-2xl text-white font-display font-bold text-sm cursor-pointer disabled:opacity-50 disabled:cursor-not-allowed transition-all">
        {/* Content */}
      </div>
    </div>
  );
};

export default LoginPage;